# `.github/public-mirror/`

Plumbing for the read-only public mirror at
[`TheVoskamps/global-claude-config-public`](https://github.com/TheVoskamps/global-claude-config-public).

## Contents

- `paths.allowlist` — paths shipped to the mirror by `git-filter-repo`.
  Anything not on this list is excluded.
- `mailmap` — rewrites private commit-author emails to a
  `<localpart>@global-claude-config.local` form for the mirror.
- `replacements.txt` — `git-filter-repo` replacement rules used for
  **both** `--replace-text` (blob content) and `--replace-message`
  (commit messages). This is the value-scrubbing pass: it rewrites
  specific values (personal paths, AWS account IDs, SSO profile names,
  cluster/repo/org names, and the `ExampleProject`/`example-` prose
  tokens) out of the content that *does* ship, as defense in depth on
  top of the path allowlist. Ordering matters — filter-repo applies
  rules in file order, so compound identifiers (full
  `<RoleName>-<AccountID>` SSO profiles, full cluster names) are listed
  before any prefix or substring that would also match them, and the
  blanket prose sweeps come last. See the comment header in the file.
  **The workflow does not feed this file to filter-repo directly.**
  filter-repo's `--replace-text` / `--replace-message` parsers do not
  skip `#`-prefixed lines (unlike `--paths-from-file`), so a bare `#`
  comment line would parse as the literal rule `# -> ***REMOVED***` and
  corrupt every `#` in every mirrored blob and commit message
  (shebangs, comments, markdown headings — issue #157). The workflow
  therefore keeps only real rule lines via an allow-list grep
  (`grep -E '^(literal:|regex:|glob:)'`) into an ephemeral
  `replacements.clean.txt` and points both flags at that cleaned copy.
  Because every rule carries an explicit `literal:`/`regex:`/`glob:`
  prefix, the allow-list both drops comments and blank lines and lets a
  rule's match-text safely begin with `#` (a deny-list `grep -v '^#'`
  would wrongly strip such a rule). The cleaned file is a runtime CI
  artifact: it is written under this directory (which is excluded from
  the allowlist), is `.gitignore`d so it can never be accidentally
  committed, and never reaches the mirror. The workflow's input
  fingerprint hashes this cleaned rule set, so a comment-only edit to
  `replacements.txt` does not trigger a refilter and force-push.
- `CONTRIBUTORS.template` — header text prepended to the auto-generated
  `CONTRIBUTORS` file. The workflow rebuilds the file content on every
  run and only commits when the resulting blob differs from the one at
  the current `HEAD:CONTRIBUTORS` (so a run with no new contributors
  produces no new commit).
- `../workflows/public-mirror.yml` — the GitHub Actions workflow that
  filters the repo, regenerates `CONTRIBUTORS` (skipped when the blob
  is unchanged), synthesizes a `.gitignore` from this allowlist
  (skipped when the blob is unchanged), and pushes to the mirror's
  `main` (skipped when the new local tip already equals the mirror's
  current tip), plus three bookkeeping branches on the mirror that
  persist filter-repo state across runs.

### Workflow behavior

- Stable public SHAs across runs. The filter step runs
  `git filter-repo --state-branch filter-repo-state
  --prune-empty=never`, so unchanged source commits keep their
  previously-computed public SHAs across runs. A genuine source
  change appends commits as a fast-forward; a consumer's `git pull`
  fast-forwards cleanly instead of seeing the entire history
  rewritten.
- Three bookkeeping branches live on the mirror (not in this repo):
  - `filter-repo-state` — owned by `git filter-repo`; holds the
    persisted fast-import marks (source-commit → public-commit SHA
    mapping).
  - `filter-repo-meta` — owned by the workflow; holds a single
    `inputs.sha256` blob with a sha256 fingerprint of
    `paths.allowlist` + `mailmap` + `replacements.txt`. Used to detect
    config changes that invalidate the persisted marks.
  - `filter-repo-blobs` — owned by the workflow; holds a synthetic
    commit whose tree anchors unreachable replacement blobs created by
    filter-repo's `--replace-text`. These blobs get marks in
    `target-marks` but have no tree entry in the filtered history, so
    without this anchor they would be lost between runs (git's
    transfer protocol only sends reachable objects). Built after each
    filter-repo run by type-classifying `target-marks` entries with
    `git cat-file --batch-check` and placing only blob-type entries
    into the tree. Pinned dates make the commit SHA deterministic when
    the blob set is unchanged.
  All three are always pushed (with `--force`) at the end of each run
  so the next run can read them back. The state branch is rewritten by
  filter-repo on a refilter; the meta branch is rebuilt fresh each
  run with pinned author/committer dates, so a no-op meta push is a
  true no-op when the fingerprint is unchanged. The blob-anchor push
  is conditional on blobs existing in target-marks.
- Editing `paths.allowlist`, `mailmap`, or `replacements.txt` triggers
  a detected full refilter on the next run. The workflow logs three
  `::warning::` annotations together: the reason
  (one of "first run (no state branch on mirror)", "meta branch
  missing or unreadable on mirror", or "filter inputs changed
  (paths.allowlist, mailmap, or replacements.txt edited)"), the
  consequence
  ("force-push the mirror's main and rewrite all public SHAs"), and
  the consumer action ("one-time
  `git fetch && git reset --hard origin/main`"). Steady-state runs
  (no source change, no config change) print
  `Filter inputs unchanged; reusing persisted marks from state branch.`
  and produce a no-op push to `main`.
- `--prune-empty=never` is a deliberate trade-off paired with
  `--state-branch` (filter-repo's source warns that empty-commit
  pruning does not work well with `--state-branch`). Source commits
  that touch only excluded paths (`.claude/`, `designs/`, `memory/`,
  etc.) show up in the public history as empty commits with the
  original commit message intact. Leaking both activity timing AND
  the commit-message contents for changes touching only excluded
  paths is the accepted cost for SHA stability; the excluded diff
  payload itself stays excluded.
- The workflow synthesizes a mirror-specific `.gitignore` from
  `paths.allowlist`. The transform strips comments and blanks, prefixes
  each surviving path with `!/` to form a negation rule, and prepends
  `/*` to ignore everything by default. The result un-ignores exactly
  the shipped set plus the `.gitignore` itself, so a user who clones
  the mirror underneath `~/.claude` gets a clean `git status` without
  the surrounding runtime clutter. The synthesis uses the same
  deterministic pattern as `CONTRIBUTORS`: build the blob, compare to
  `HEAD:.gitignore`, skip the synthetic commit when unchanged. Because
  the `.gitignore` is produced downstream from the allowlist, the
  allowlist itself must NOT list `.gitignore` — the allowlist drives
  the synthesis, not the other way around.
- The first run after this change lands is a one-time SHA-scheme
  switch: filter-repo has no persisted marks yet, so it rebuilds
  history from scratch and force-pushes a rewritten `main`.
  Consumers reset once; subsequent runs are fast-forward.
- The bare clone is made with `git clone --no-local --bare
  "file://$PWD/.git"` so it is single-packed before filter-repo runs.
  filter-repo's from-scratch path enforces a freshness check
  ("expected freshly packed repo") that requires a single pack
  (packs <= 1) with no non-replace-ref loose objects. A plain
  `git clone --bare .` from the Actions checkout takes git's
  local-clone hardlink path, which mirrors the source's pack layout;
  the Actions checkout is multi-packed, so the clone ends up with two
  packs and trips the check's `packs > 1` clause, aborting the run
  before any history is rewritten (issue #160). `--no-local` via a
  `file://` URL forces the real transfer protocol, which sends a
  single packfile (count 0, packs 1); the `file://` form is required
  because a plain path can still trigger the local-clone optimization
  even with `--no-local` on some git versions. This supersedes the
  earlier `git gc` remediation for the multi-pack problem (issues
  #160/#162). The workflow does NOT pass `git filter-repo --force` —
  the maintainer argues against bypassing the check, and making the
  clone genuinely fresh is the correct fix. The clone keeps a single
  remote named `origin`, so it leaves the one-remote-named-`origin`
  constraint (above) intact.
- **Root cause of post-clone freshness failures (confirmed)**: the
  `--no-local` clone produces a pristine single-pack bare repo
  (count 0, packs 1), but the two `git fetch "$MIRROR_URL"` calls
  that restore the persisted state and meta branches write **loose
  objects** into the clone (the fetch transfer protocol for a
  single-ref fetch often stores objects loose rather than as a
  packfile). filter-repo's freshness check then sees `count > 0`
  and aborts. This was confirmed by the temporary #183 probe (issue
  #183), which measured three candidate cleanups on identical
  post-fetch clones on the actual runner; cycle 3
  (`git gc --prune=now`) yielded the required count 0 / packs 1
  state. The earlier #160/#162 `git gc` approach failed not because
  gc was wrong, but because it ran *before* the state/meta fetches
  (commit `25e051f` placed it at the identity-config block) — the
  fetches then re-introduced loose objects after gc had cleaned them.
- **The fix**: `git gc --prune=now` runs immediately after the
  state/meta fetches and before the diagnostics block / filter-repo
  invocation. `gc --prune=now` is the widest cleanup tool: it
  normalizes packs, loose objects, reflogs, and refs in one pass —
  the maintainer-prescribed remediation per filter-repo issue #38,
  which covers more of the freshness heuristic than `git repack -ad`
  alone. `--prune=now` removes any dependence on the 2-week default
  prune grace period. On a disposable bare clone that exists only to
  be filtered and discarded, the usual cost/risk of the wide tool is
  nil, while the benefit is robustness against freshness conditions
  we have not yet observed (the full heuristic is ~12 conditions; the
  only one observed firing was loose objects, but the entire history
  of this bug is unseen conditions biting after the fact). References:
  #160, #162, #163, #181, #183, #186.
- The filter step prints a **permanent, read-only diagnostics block**
  immediately before `git filter-repo` runs (issue #181). When the
  from-scratch path aborts with "this does not look like a fresh
  clone", filter-repo does not say *which* of its ~12 freshness
  conditions was violated, and off-runner reproduction has repeatedly
  disagreed with the actual runner state (issues #160/#162,
  #163/#164). The block dumps the runner's own pre-filter inputs at
  the exact point filter-repo checks them — `git rev-parse
  --git-dir --is-bare-repository`, `core.ignorecase` /
  `core.precomposeunicode`, `git count-objects -v`, `git remote -v`,
  reflog entry counts, the `refs/stash` ref, and total ref count — so
  a real run reports the violated condition directly instead of
  inferring it. After the `git gc --prune=now` fix above, the
  diagnostics block prints count 0, packs 1 on a normal run —
  confirming the clone is fresh rather than diagnosing why it is not.
  Every probe is read-only (no `gc`/`repack`/`prune`/`fetch`/config
  write) and guarded so it cannot abort the step before filter-repo
  gets to run; the block has zero effect on the clone, the filter, or
  the public output. It stays in permanently rather than being added
  and removed per investigation.

### Incremental mechanism

`--state-branch` incremental filtering requires that all objects
referenced in `target-marks` exist in the bare clone's object store at
the time filter-repo runs. The workflow builds a fresh bare clone from
source on every run, so target-side objects (which only exist on the
mirror) must be fetched back in.

Two categories of target objects need anchoring:

1. **Reachable target objects** (commits, trees, blobs that are part of
   the mirror's `main` history). The workflow fetches the mirror's
   `refs/heads/main` into `refs/heads/_mirror_anchor` in the bare
   clone. This brings all reachable target-side objects into the object
   store.

2. **Unreachable replacement blobs**. filter-repo's `--replace-text`
   creates scrubbed versions of every blob it processes, including
   blobs whose paths are excluded by `--paths-from-file`. These blobs
   get marks in `target-marks` but no tree entry in the filtered
   history — they are unreachable from any ref. The workflow fetches
   `refs/heads/filter-repo-blobs` into `refs/heads/_target_blobs` to
   restore these blobs.

The anchor and blob-anchor refs are local-only fetch targets
(`_mirror_anchor`, `_target_blobs`) — they are never pushed back. The
source of truth for the anchor is the mirror's own `main`; the source
of truth for the blob anchor is `filter-repo-blobs`.

`--refs refs/heads/main` scopes `git fast-export` to walk only the
source branch, not the fetched anchor/blob refs. Without it,
filter-repo's default `--all` would walk every ref in the clone,
including the anchor refs whose commits are not in `source-marks`
(they are target-side), causing fast-export to treat them as new
source commits and attempt to re-export them. The `--refs` +
`--state-branch` + `--prune-empty=never` combination is safe: the
`--state-branch` warning in filter-repo's source about `--refs` is
specifically about its interaction with empty-commit pruning, and we
already pass `--prune-empty=never`.

The anchor/blob fetches do not affect the freshness check:
`git fetch` from a URL does not add a remote (still just `origin`),
bare clones default `core.logAllRefUpdates=false` (no reflog entries),
and `git gc --prune=now` folds the fetched loose objects into the
single pack. `--refs refs/heads/main` means filter-repo does not see
the anchor refs at all.

After the anchor/blob fetches and before filter-repo runs, a
**self-healing target-marks validation** checks that the SHAs in
`target-marks` actually resolve in the clone. If a state branch exists
and none of the other refilter checks (first run, missing meta,
fingerprint mismatch) have already triggered, the workflow extracts the
SHA column from `target-marks` via `git show` and pipes them through
`git cat-file --batch-check`. If any SHA comes back as `missing`, the
marks are invalid for incremental use (e.g. a prior from-scratch
refilter orphaned the target objects) and the workflow triggers a full
refilter automatically. This makes the incremental mechanism
self-healing: no manual intervention is needed when the state branch
becomes stale.

References: #196, #197.

## Mirror posture (manually applied — POC for issue #66)

The mirror is configured as **read-only for code, open for issues**.
External users can file issues against the public repo; we triage and
fix them in the upstream private repo. PRs cannot land.

This posture was reached by hand on 2026-05-17. The configuration is
not declared as IaC in this repo — the
[`repo-public-mirror-setup`](../../skills/repo-public-mirror-setup.md)
skill (tracked by [#66](https://github.com/TheVoskamps/global-claude-config/issues/66))
is the canonical declaration. Re-running that skill against the mirror
should be **idempotent**: it should reconcile the live state to what
the skill specifies, with no manual cleanup required.

### Target configuration

On the mirror repo (`TheVoskamps/global-claude-config-public`):

- Repo settings:
  - `has_issues=true`
  - `has_projects=false`, `has_wiki=false`, `has_discussions=false`
  - `allow_merge_commit=true` (forced — see "Gotchas" below)
  - `allow_squash_merge=false`, `allow_rebase_merge=false`
  - `delete_branch_on_merge=true`
- Branch protection on `main`: **none** (replaced by ruleset; see below).
- Ruleset on `refs/heads/main`:
  - Name: `mirror-main-readonly`
  - Enforcement: `active`
  - Rules: `update`, `deletion`, `non_fast_forward` (blocks normal
    pushes, branch deletion, and force-pushes for non-bypass actors)
  - Bypass actors: repo admin (`actor_id: 5`, `actor_type:
    RepositoryRole`, `bypass_mode: always`)
- Deploy key:
  - Title: `public-mirror-workflow`
  - `read_only: false` (must be writable so the workflow can force-push)
  - Private half stored as the `PUBLIC_MIRROR_DEPLOY_KEY` secret on the
    *upstream* (private) repo

### How it was applied

```bash
# Enable Issues
gh api -X PATCH repos/TheVoskamps/global-claude-config-public \
  -f has_issues=true

# Create the ruleset (payload in JSON below)
gh api -X POST repos/TheVoskamps/global-claude-config-public/rulesets \
  --input ruleset.json

# Delete the old branch protection (rulesets supersede it)
gh api -X DELETE \
  repos/TheVoskamps/global-claude-config-public/branches/main/protection
```

Ruleset JSON:

```json
{
  "name": "mirror-main-readonly",
  "target": "branch",
  "enforcement": "active",
  "bypass_actors": [
    {
      "actor_id": 5,
      "actor_type": "RepositoryRole",
      "bypass_mode": "always"
    }
  ],
  "conditions": {
    "ref_name": {
      "include": ["~DEFAULT_BRANCH"],
      "exclude": []
    }
  },
  "rules": [
    { "type": "deletion" },
    { "type": "non_fast_forward" },
    { "type": "update" }
  ]
}
```

### Verification

After applying:

```bash
gh api repos/TheVoskamps/global-claude-config-public --jq \
  '{has_issues, has_projects, has_wiki, allow_merge_commit,
    allow_squash_merge, allow_rebase_merge}'
# expect: {has_issues: true, allow_merge_commit: true, others: false}

gh api repos/TheVoskamps/global-claude-config-public/branches/main/protection
# expect: 404 "Branch not protected"

gh api repos/TheVoskamps/global-claude-config-public/rulesets \
  --jq '.[] | {name, enforcement}'
# expect: {name: "mirror-main-readonly", enforcement: "active"}
```

Then manually trigger the mirror workflow to confirm the deploy-key
push still passes through the ruleset:

```bash
gh workflow run public-mirror.yml --ref main
```

### Gotchas (for the skill to handle)

1. **GitHub refuses to disable all three merge methods.** A
   `PATCH` with `allow_merge_commit=false, allow_squash_merge=false,
   allow_rebase_merge=false` returns:

   > "Sorry, you need to allow at least one merge strategy."
   > `(no_merge_method)`

   So we leave `allow_merge_commit=true`. The PR Merge button is *not*
   greyed out — but the ruleset on `main` blocks any PR merge from
   landing anyway, so this is harmless. The skill should pick one
   merge method (merge-commit is the least disruptive) and leave the
   others off.

2. **Deploy keys with write access bypass branch rulesets natively.**
   Our `bypass_actors` configuration uses `RepositoryRole id 5`
   (admin). The workflow's force-push succeeded under the new ruleset
   even though the deploy key is not associated with any repository
   role — rulesets don't enforce against deploy keys the same way they
   do against users. This means the admin-role bypass is
   **belt-and-suspenders**, not load-bearing: the workflow would still
   work without it. We keep it so that if the workflow auth ever
   migrates to a PAT or app installation owned by an admin user, the
   bypass is already in place.

3. **PRs cannot be disabled at the repo level.** GitHub has no
   `has_pull_requests` toggle. External users can still open PRs
   against the mirror; the merge will fail because the push to `main`
   is blocked by the ruleset. The README banner tells them to file
   issues instead and explains that fixes happen upstream.

4. **Rulesets supersede branch protection.** We delete the
   pre-existing branch protection after creating the ruleset. Leaving
   both in place is allowed by GitHub but causes confusing
   double-evaluation. The skill should delete branch protection if
   present whenever the ruleset is reconciled.

5. **The allowlist must not ship `.github/`.** The mirror workflow's
   force-push uses a deploy key, which lacks the `workflow` OAuth
   scope. Pushing a commit that touches `.github/workflows/` would
   fail with:

   > "refusing to allow an OAuth App to create or update workflow ...
   > without `workflow` scope"

   So `.github/` is excluded from the allowlist by design, which means
   this directory (`.github/public-mirror/`) — including this README —
   never ships to the mirror. That's correct: it's upstream-only
   plumbing.

### What the skill needs to do (issue #66)

When run against a fresh repo: create everything above from scratch.
When re-run against an existing mirror: reconcile each piece
idempotently — skip if matched, update if drifted, delete if extra
(e.g. a recreated branch protection). The skill's first phase
(deploy-key creation, workflow file installation, allowlist+mailmap
templates) is already idempotent in the existing implementation; the
posture configuration above is the gap this issue closes.
